# Test server behavior configuration
# Changes take effect immediately — no restart needed.

# Simulate login delay in seconds (use a high value to trigger Playwright timeouts)
login_delay_seconds=0

# Set to false to simulate a failed upload
upload_success=true

# Maximum number of files to process per run (0 = no limit)
batch_size=5
